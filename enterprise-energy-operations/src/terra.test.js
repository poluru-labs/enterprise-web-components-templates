import { beforeAll, expect, test } from 'vitest';
import { JSDOM } from 'jsdom';
Object.defineProperty(window, 'localStorage', {value: new JSDOM('', {url:'https://terra.test'}).window.localStorage, configurable:true});
beforeAll(async()=>{document.body.innerHTML='<div id="app"></div>';await import('./terra.js');});
test('portfolio renders six distinct modules and correct site details',()=>{expect(document.querySelector('h1').textContent).toBe('Portfolio overview');expect(document.querySelectorAll('[data-nav]')).toHaveLength(7);document.querySelector('[data-site="Suncrest Solar Park"]').click();expect(document.querySelector('#detail-drawer').open).toBe(true);expect(document.querySelector('#drawer-content').textContent).toContain('Arjun Poluru');});
test('site filters and search combine with an empty state',()=>{const filter=document.querySelector('#type-filter');filter.dispatchEvent(new CustomEvent('eds-change',{detail:{value:'Wind'}}));expect(document.querySelectorAll('#site-rows tr')).toHaveLength(2);document.querySelector('#site-search').dispatchEvent(new CustomEvent('eds-input',{detail:{value:'unknown'}}));expect(document.querySelector('#site-rows eds-empty-state')).not.toBeNull();});
test('mega menu toggles its accessible state',()=>{document.querySelector('#mega-toggle').click();expect(document.querySelector('#mega-menu').hidden).toBe(false);expect(document.querySelector('#mega-toggle').getAttribute('aria-expanded')).toBe('true');});
test('maintenance requires a title and persists valid tasks',()=>{document.querySelector('[data-action="save-work"]').click();expect(document.querySelector('#form-error').textContent).toContain('Enter a task name');document.querySelector('#work-title').value='Quarterly safety inspection';document.querySelector('[data-action="save-work"]').click();const tasks=JSON.parse(window.localStorage.getItem('terra-work-orders'));expect(tasks[0].title).toBe('Quarterly safety inspection');expect(tasks[0].owner).toMatch(/Poluru$/);});
